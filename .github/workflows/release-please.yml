name: Release Please

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      dry_run:
        type: boolean
        default: false
        description: "Preview the release PR diff without opening or updating it"

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    name: Open or update Release PR
    runs-on: ubuntu-latest
    steps:
      - uses: googleapis/release-please-action@v4
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json
          dry-run: ${{ inputs.dry_run || false }}
