/** Type surface for the pure halves of check-ratchets.mjs (unit-tested from examples/test). */
export function compareRatchets(relPath: string, oldJson: unknown, newJson: unknown): string[]
export function monitoredFiles(root: string): string[]
